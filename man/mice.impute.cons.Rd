% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mice-impute-cons.R
\name{mice.impute.cons}
\alias{mice.impute.cons}
\title{Constrained Multiple Imputation: core (positional/global constraint) interface}
\usage{
mice.impute.cons(
  y,
  ry,
  x,
  wy = NULL,
  nnet.maxit = 100,
  nnet.trace = FALSE,
  constrain = NULL,
  nnet.MaxNWts = 1500,
  ...
)
}
\arguments{
\item{y}{A vector to impute (factor or coercible to character).}

\item{ry}{Logical vector indicating observed (\code{TRUE}) vs missing
(\code{FALSE}) entries of \code{y}.}

\item{x}{Predictor matrix (numeric or coercible via \code{as.matrix}).}

\item{wy}{Optional logical vector indicating which observations to impute;
defaults to \code{!ry}.}

\item{nnet.maxit}{Maximum iterations for \code{nnet::multinom} (default 100).}

\item{nnet.trace}{Logical; passed to \code{nnet::multinom} to toggle fitting
trace output (default \code{FALSE}).}

\item{constrain}{Optional vector of category labels that are structurally
inadmissible for the imputed observations. Matched against the category
labels of \code{y}; constrained categories receive zero probability before
sampling. \code{NULL} (the default) applies no constraint.}

\item{nnet.MaxNWts}{Maximum number of weights allowed in
\code{nnet::multinom} (default 1500).}

\item{...}{Additional arguments passed to \code{nnet::multinom}.}
}
\value{
A character vector of imputed values for the missing entries
  (length \code{sum(wy)}). Returns \code{NA_character_} for rows where the
  constraint set leaves no admissible category.
}
\description{
Imputes missing categorical values under structural constraints by fitting a
weighted multinomial model (\code{nnet::multinom}) on the observed and
augmented data, then zeroing the predicted probability of every constrained
(inadmissible) category before row-wise sampling. This is the low-level
engine; for label-validated constraints use the recommended
\code{\link{mice.impute.cons_named}} wrapper, which checks \code{exclude}
against \code{levels(y)} before delegating here.
}
\details{
Compatible with the standard \code{mice()} / \code{with()} / \code{pool()}
pipeline: register it as a custom \code{method} for the column to impute.
}
\seealso{
\code{\link{mice.impute.cons_named}} for the label-validated
  interface.
}
