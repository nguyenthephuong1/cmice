% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cmice-package.R
\name{mice.impute.cons_named}
\alias{mice.impute.cons_named}
\title{Constrained Multiple Imputation: named-category interface}
\usage{
mice.impute.cons_named(
  y,
  ry,
  x,
  wy = NULL,
  exclude = character(0),
  nnet.maxit = 100,
  nnet.MaxNWts = 1500,
  ...
)
}
\arguments{
\item{y}{A factor vector to impute. Must have \code{levels(y)} defined.}

\item{ry}{Logical vector indicating observed (TRUE) vs missing (FALSE).}

\item{x}{Predictor matrix (numeric or coerced).}

\item{wy}{Optional logical vector indicating which observations to impute
(defaults to \code{!ry}).}

\item{exclude}{Character vector of category labels in \code{levels(y)} that
are structurally inadmissible for the missing observations being imputed.
Every label must appear in \code{levels(y)}; mismatches raise an error.}

\item{nnet.maxit}{Maximum iterations for \code{nnet::multinom} (default 100).}

\item{nnet.MaxNWts}{Maximum number of weights for \code{nnet::multinom}
(default 1500).}

\item{...}{Additional arguments passed to \code{nnet::multinom}.}
}
\value{
A character vector of imputed values for the missing entries.
}
\description{
Imputes missing categorical values under structural constraints. The
\code{exclude} argument names categories that are inadmissible and will
receive zero predicted probability before sampling. This is the recommended
interface; it validates labels against \code{levels(y)} at call time.
}
\examples{
\dontrun{
# Define a custom imputation method that excludes "SMC" by name
mice.impute.cons_no_smc <- function(y, ry, x, wy = NULL, ...) {
  mice.impute.cons_named(y, ry, x, wy, exclude = c("SMC"), ...)
}
}
}
\seealso{
\code{\link{mice.impute.cons}} (legacy positional interface).
}
